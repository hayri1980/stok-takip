name: keepalive
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render health
        run: curl -s -o /dev/null -w "%{http_code}\n" https://stok-takip-logu.onrender.com/health
