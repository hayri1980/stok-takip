services:
  - type: web
    name: stok-takip
    runtime: node
    plan: free
    rootDir: stok-takip-upload
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
